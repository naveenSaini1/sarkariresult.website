

export const metadata = {
    title: "Search",
    description: "Search The Box",
  };
  
  export default function SearchPageLayout({ children }) {
    return (
        <>
              {children}
        </>
    );
  }
  