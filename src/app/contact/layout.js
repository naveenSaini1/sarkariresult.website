

export const metadata = {
    title: "Contact",
    description: "Find The Contact",
  };
  
  export default function ContactLayout({ children }) {
    return (
        <>
        {children}
         </>
    );
  }
  