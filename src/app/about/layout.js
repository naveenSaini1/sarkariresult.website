

export const metadata = {
    title: "About",
    description: "Find The About",
  };
  
  export default function AboutLayout({ children }) {
    return (
        <>
        {children}
         </>
    );
  }
  