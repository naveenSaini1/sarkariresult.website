

export const metadata = {
    title: "Syllabus",
    description: "Find The Syllabus",
};

export default function SyllabusLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}
